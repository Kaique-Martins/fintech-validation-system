{
  "name": "fintech-validation-system",
  "version": "2.0.0",
  "description": "Autonomous Data Validation Agent for FinTech Credit Risk Analysis",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run start:dev\" \"cd frontend && npm run dev\"",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "start": "npm run start:prod --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
